# Pontos de entrada da sua API
type Query {
  ola: String
  horaAtual: Date
  usuarioLogado: Usuario
  precoEmDestaque: Produto
  numerosMegaSena: [Int!]!
  usuarios: [Usuario]
  usuario(id: Int): Usuario

  perfis: [Perfil]
  perfil(
    filtro: PerfilFiltro!
  ): Perfil
}